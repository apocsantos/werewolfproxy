# Candidate release metadata. Cargo package versions remain unchanged during
# Stage19 so the locked dependency graph and supported persistence schema stay
# byte-for-byte identical. This value is injected only into release binaries.
RELEASE_VERSION=1.0.0-rc.1
RELEASE_TARGET=x86_64-unknown-linux-gnu
PRODUCTION_CODE_COMMIT=48b9f9ad0a37b91645fe7ed939892a4b8fef60dd
